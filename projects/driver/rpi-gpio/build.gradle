
def projectName = "OpenMUC Driver - Raspberry Pi GPIO"

configurations.create('embed')

def pi4jVersion = '1.2'

dependencies {
  implementation project(':openmuc-core-spi')

  implementation group: 'com.pi4j', name: 'pi4j-core', version: pi4jVersion
  embed group: 'com.pi4j', name: 'pi4j-core', version: pi4jVersion

  implementation group: 'com.pi4j', name: 'pi4j-device', version: pi4jVersion
  embed group: 'com.pi4j', name: 'pi4j-device', version: pi4jVersion

  implementation group: 'com.pi4j', name: 'pi4j-gpio-extension', version: pi4jVersion
  embed group: 'com.pi4j', name: 'pi4j-gpio-extension', version: pi4jVersion
}

jar {
  manifest {
    name = projectName
    instruction 'Bundle-ClassPath', '.,lib/pi4j-core-'+pi4jVersion+'.jar,lib/pi4j-device-'+pi4jVersion+'.jar,lib/pi4j-gpio-extension-'+pi4jVersion+'.jar'
    instruction 'Import-Package', '!com.pi4j.*,*' + jarDefaultImportPackageVersion
    instruction 'Export-Package', ''
  }
}

jar {
  into('lib') {
    from configurations.embed
  }
}
