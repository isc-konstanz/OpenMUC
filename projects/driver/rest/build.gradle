
def projectName = "OpenMUC Driver - REST"
def projectDescription = "REST driver for the OpenMUC framework."

configurations.create('embed')

def commonscodecversion = '1.11'
def gsonversion = '2.8.6'

dependencies {
  implementation project(':openmuc-driver')
  implementation group: 'com.google.code.gson', name: 'gson', version: gsonversion
  implementation group: 'commons-codec', name: 'commons-codec', version: commonscodecversion
  embed group: 'commons-codec', name: 'commons-codec', version: commonscodecversion
}

jar {
  manifest {
    name = projectName
    instruction 'Bundle-Classpath', '.,lib/commons-codec-' + commonscodecversion + '.jar'
    instruction 'Import-Package', '!org.apache.commons.codec*,*' + jarDefaultImportPackageVersion
    instruction 'Export-Package', ''
  }
}

jar {
  into('lib') {
    from configurations.embed
  }
}

uploadArchives {
  repositories {
    mavenDeployer {
      pom.project {
        name projectName
        description projectDescription
      }
    }
  }
}

