def projectPackage = rootDir.getPath() + '/build/dpkg/openmuc-driver-mysql-' + cfgVersion

configurations.create("openmuc_driver_mysql")
configurations.openmuc_driver_mysql.transitive = false;

repositories {
    mavenCentral()
    flatDir {
        dirs rootDir.getPath() + '/build/libs-all'
    }
}
dependencies {
    openmuc_driver_mysql project(':openmuc-driver-sql')
    openmuc_driver_mysql group: 'mysql', name: 'mysql-connector-java', version: "8.0.17"
}
copy {
    from project.configurations.openmuc_driver_mysql
    into projectPackage + '/bundle'
}
