copy {
  from project.configurations.osgibundles
  into rootDir.getPath() + '/build/dpkg/openmuc' + cfgDebianDir + '/bundle'
  exclude 'openmuc*'
  exclude 'gson*'
}
copy {
  from project.configurations.osgimain;
  into rootDir.getPath() + '/build/dpkg/openmuc' + cfgDebianDir + '/bin'
  rename (/org.apache.felix.main-.*jar/, "felix.jar")
}
